<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Книга рецептов</title>
    <link rel="stylesheet" type="text/css" href="css/index.css">
</head>
<body>
    <!-- Шапка сайта -->
    <header>
        <img src="home_icon.png" alt="Главная" width="30" height="30"> <!-- Значок "Домой" -->
        <h1>Кулинарные рецепты</h1> <!-- Название сайта -->
        <img src="profile_icon.png" alt="Профиль" width="30" height="30"> <!-- Значок "Профиль" -->
    </header>

    <div class="content">
        <!-- Боковая навигационная панель -->
        <nav class="sidebar">
            <input type="text" placeholder="Поиск"> <!-- Поле поиска -->
            <h3>Категории</h3> <!-- Заголовок для категорий -->
            <ul>
                {{#each WebPagesArray}}
                    <li><a href={{this}}>{{this}}</a></li>
                {{/each}}
            </ul>
        </nav>
            <!-- Основной контент -->
        <main class="main-content">
            <h2 class="top-5">Топ-5 блюд</h2> <!-- Заголовок "Топ-5 блюд" -->
            
            <!-- Один блок с рецептом -->
            {{#each TopParamsArray}}
            <div class="recipe">
                <img src={{this.IMG}} alt="IMG"> <!-- Заставка для картинки -->
                <div>
                    <h3>{{this.Title}}</h3> <!-- Название блюда -->
                    <p>{{this.Desctiption}}</p> <!-- Описание блюда -->
                </div>
            </div>
            {{/each}}
    {{{body}}}
</body>
</html>