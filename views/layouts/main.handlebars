<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Кулинарные рецепты - Главная</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;700&display=swap">
    <link rel="stylesheet" type="text/css" href="css/index.css">
</head>
<body>
    <header class="header">
        <h1>Кулинарные рецепты</h1>
        <p>Найдите свои любимые блюда или поделитесь своими рецептами</p>
    </header>

    <nav class="navigation">
        <ul class="navigation-list">
            <li><a href="/">Главная</a></li>
            {{#each NavigationItems}}
                <li><a href="{{this.path}}">{{this.title}}</a></li>
            {{/each}}
            <li><a href="/inputpage">Добавить рецепт</a></li>
        </ul>
    </nav>

    <div class="container">
        <section class="top-recipes">
            <h2 class="section-title">Наши лучшие рецепты</h2>
            
            <div class="recipes-grid">
                {{#each TopParamsArray}}
                <a href="{{this.path}}" class="recipe-link">
                    <div class="recipe-card">
                    <div class="recipe-image" style="background-image: url('{{this.IMG}}')"></div>
                    <div class="recipe-content">
                        <h3 class="recipe-title">{{this.Title}}</h3>
                        <p class="recipe-description">{{this.Description}}</p>
                    </div>
                    </div>
                </a>
                {{/each}}
            </div>
        </section>

        <a href="/inputpage" class="add-recipe-btn">Добавить свой рецепт</a>

        {{{body}}}
    </div>

    <footer class="footer">
        <p>&copy; 2025 Кулинарные рецепты.</p>
    </footer>
</body>
</html>
